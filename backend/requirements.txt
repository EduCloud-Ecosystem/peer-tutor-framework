# Core backend dependencies.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
SQLAlchemy>=2.0
psycopg[binary]>=3.1      # required for postgresql+psycopg:// DSN (pilot Postgres)

# Model layer (OpenAI-compatible client for the Jetstream2 Inference Service).
openai>=1.40
spacy>=3.7
# Alternate provider (only needed if LLM_PROVIDER=anthropic):
# anthropic>=0.39

# Domain-pack dependencies (the data-science pack) live in requirements-packs.txt:
#       pip install -r requirements-packs.txt
